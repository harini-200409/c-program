#include <stdio.h>

int main()
{
    int a;
    printf("a:");
    scanf("%d",&a);
    int n1=10;
    for(int i=0;i<a;i=i+2)
    {
        n1=n1+5;
        printf("%d ",n1);
        if(i<a-1)
        printf("%d ",i);
    }
}
