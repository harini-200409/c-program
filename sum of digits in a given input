// sum of digits in a given input 
#include <stdio.h>
#include<string.h>
int main()
{
    int a,sum=0,q;
    printf("a=");
    scanf("%d",&a);
    while(a>0)
    {
        q=a%10;
        sum=sum+q;
        a=a/10;
    }
    printf("%d",sum);
}
