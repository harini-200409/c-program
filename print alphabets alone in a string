#include <stdio.h>
#include<string.h>
int main()
{
    int i;
   char a[10];
   printf("a:");
   scanf("%s",a);
   int l;
   l=sizeof(a);
   for(i=0;i<l;i++)
   {
       if(a[i]>=97&&a[i]<=122)
       {
           printf("%c",a[i]);
       }
   }
}

